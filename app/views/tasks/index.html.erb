<div class="container mt-5">

  <h1 <i class="fa-solid fa-hippo"></i> My tasks </h1>
  <ul>
    <% @tasks.each do |task| %>
      <li><%= link_to task.title, task_path(task) %></li>
    <% end %>
  </ul>
<p><%= link_to "Add a new task", tasks_new_path%></p>

</div>
